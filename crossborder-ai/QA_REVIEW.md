# QA 审查报告

> 时间：2026-07-29
> 范围：前端 UI、后端 API、整体体验

---

## 一、技术性检查

| 检查项 | 结果 | 说明 |
|--------|------|------|
| 所有 JS 文件加载 | ✅ 200 OK | 无 404 |
| 所有 CSS 加载 | ✅ 200 OK | 无 404 |
| 后端健康检查 | ✅ healthy | 数据库连接正常 |
| API 文档 | ⚠️ 生产关闭 | DEBUG=false 时正常 |
| HTTPS | ⚠️ 未开启 | 未备案前正常 |

---

## 二、UI / 用户体验问题

### 2.1 待修复问题

| # | 问题 | 位置 | 严重度 | 建议 |
|---|------|------|--------|------|
| 1 | **Nginx 配置中域名未替换** | nginx/nginx.conf | ⚠️ 中 | 仍有 `example.com` 注释残留 |
| 2 | **页面标题仍是 VeyaShip（无 AI）** | index.html | ⚠️ 中 | `<title>VeyaShip</title>` → `<title>VeyaShip AI</title>` |
| 3 | **首页 description meta 未更新** | index.html | ⚠️ 低 | 仍是 AI-powered cross-border e-commerce platform，未体现选品决策 |
| 4 | **中日韩等语言本地化文件存在但未维护** | i18n/locales/ | ⚠️ 中 | ja.json、ko.json 等翻译不完整 |
| 5 | **第一次加载时无骨架屏** | 所有页面 | ⚠️ 中 | SPA 加载期间白屏，缺少 loading 状态 |
| 6 | **Agent 中英文混合** | AgentPage | ⚠️ 低 | 部分提示文字中英夹杂 |

### 2.2 功能性问题

| # | 问题 | 位置 | 严重度 | 建议 |
|---|------|------|--------|------|
| 7 | **Netlify/部署状态页缺失** | 全局 | ℹ️ 低 | 缺少 /status 页 |
| 8 | **品类分析结果 UI 为纯文本** | Dashboard | ⚠️ 中 | 报告目前显示为 `<pre>` 文本，应改为排版更好的 Markdown 渲染 |
| 9 | **Agent 历史对话 UI 未直接展示** | AgentPage | ⚠️ 低 | 对话记录通过 API 获取，但 UI 未自动加载历史 |
| 10 | **Shopify 订单时间格式** | ShopifyPage | ℹ️ 低 | 建议改为相对时间（"2 小时前"） |
| 11 | **Dashboard 统计卡片"AI 助手"** | Dashboard | ⚠️ 中 | AI 助手作为统计项不合理，应改为"已分析品类数" |
| 12 | **Landing 页定价卡片缺失"按次付费"** | LandingPage | ℹ️ 低 | 建议增加按次付费（¥9.9/份） |

### 2.3 小细节

| # | 问题 | 严重度 |
|---|------|--------|
| 13 | 移动端汉堡菜单图标可能被浏览器收藏夹图标遮挡 | ℹ️ 低 |
| 14 | Logo 点击应返回首页（当前无跳转） | ⚠️ 低 |
| 15 | 商品卡片价格小数点后两位始终显示（¥49.00） | ℹ️ 低 |
| 16 | 黑暗模式下部分卡片对比度不足 | ℹ️ 低 |

---

## 三、需要解决的高优先级问题

### P0（影响核心体验）

暂无

### P1（应尽快修复）

| # | 问题 | 修复方案 |
|---|------|---------|
| **1** | Bundle 体积大 (vendor 165KB) | 可做代码分割，但暂不影响功能 |
| **8** | 品类分析结果展示为纯文本 | 改用 Markdown 渲染组件 |
| **11** | Dashboard 统计项"AI助手"不合理 | 改为显示"已分析品类"或"今日分析" |

### P2（后续迭代）

| # | 问题 |
|---|------|
| 5 | SPA 白屏加载 |
| 9 | Agent 自动加载历史对话 |
| 12 | 按次付费入口 |

---

## 四、优化建议（非阻塞）

1. **性能**：首页 Lighthouse 测试待做（目前无缓存策略外的优化）
2. **SEO**：Landing 页缺少 structured data
3. **监控**：缺少 Sentry 或类似错误监控
4. **分析**：缺少 Google Analytics 或类似统计
5. **PWA**：未配置 Service Worker，无法离线访问
